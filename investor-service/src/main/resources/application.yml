server:
  port: 8081
spring:
  application:
    name: investor-service
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/fundwise_investor?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    username: root
    password: root123
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
